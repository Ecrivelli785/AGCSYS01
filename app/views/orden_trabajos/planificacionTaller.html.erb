<div class="d-flex justify-content-center">
  <a class=" btn btn-warning text-center my-3" type="button" href="/planificacionTallerPDF.pdf"  target='blank' ><h4><strong>DESCARGAR PDF</strong></h4> </a>
</div>
<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>SM en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "SM" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque" and cadenatovector(orden_trabajo.procesos) != "PM"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>SM LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "SM" and orden_trabajo.estado_actual.include? "Impresión" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>

<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>PM en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "PM" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>PM LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "PM" and orden_trabajo.estado_actual.include? "Impresión" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>
<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>GTO en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "GTO" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>GTO LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "GTO" and orden_trabajo.estado_actual.include? "Impresión" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>
<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>KO en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Ko" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>KO LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Ko" and orden_trabajo.estado_actual.include? "Impresión" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>
<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Xe en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Xe" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Xe LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Xe" and orden_trabajo.estado_actual.include? "Impresión" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>
<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Sorz en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "SORZ" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Sorz LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "SORZ" and orden_trabajo.estado_actual.include? "Impresión" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>

<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Sorz en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "SORZ" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Sorz LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "SORZ" and orden_trabajo.estado_actual.include? "Impresión" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>

<h1 class="m-3 bg-primary text-center p-1 text-white"><strong>TERMINACIÓN</strong></h1>


<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Tipografía en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Tipografia" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Tipografía LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Tipografia" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>




<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Binder en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Binder" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Binder LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Binder" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>



<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Duplo en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Duplo" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Duplo LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Duplo" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>





<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Laminadora en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Lam" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Laminadora LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Lam" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>





<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Zechini en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Zechini" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Zechini LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Zechini" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>




<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Baum en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Baum" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Baum LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Baum" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>




<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Fidia en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Fidia" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Fidia LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Fidia" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>




<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Buv en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Buv" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Buv LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Buv" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>


<div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-danger text-white"><strong>Renz en espera</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Renz" and orden_trabajo.estado_actual != "Impresión" and orden_trabajo.estado_actual != "Pendiente" and orden_trabajo.estado_actual != "Terminación" and orden_trabajo.estado_actual != "Empaque"%>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3 p-1 bg-success text-white"><strong>Renz LISTO</strong></h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
       <% if cadenatovector(orden_trabajo.procesos).include? "Renz" and orden_trabajo.estado_actual.include? "Terminación" %>
          <tr>
            <td><%= orden_trabajo.clinom %></td>
            <td><%= orden_trabajo.nomprod.first(20) %></td>
            <td><%= orden_trabajo.deadline.try(:strftime, ("%e %B")) %></td>
            <td>
        <% end %>
      <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>


